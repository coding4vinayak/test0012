<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - AI Code Roaster</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <nav class="navbar">
    <div class="nav-brand">
      <a href="/"><span class="logo">&gt;_</span> Code Roaster</a>
    </div>
    <div class="nav-links">
      <a href="/auth/register">Sign Up</a>
    </div>
  </nav>

  <main class="container auth-page">
    <div class="auth-card">
      <h1>Welcome Back</h1>
      <p class="auth-subtitle">Login to view your roast history</p>

      <% if (error) { %>
        <div class="error-message"><%= error %></div>
      <% } %>

      <form method="POST" action="/auth/login" class="auth-form">
        <div class="form-group">
          <label for="email">Email</label>
          <input type="email" id="email" name="email" required placeholder="dev@example.com">
        </div>
        <div class="form-group">
          <label for="password">Password</label>
          <input type="password" id="password" name="password" required placeholder="********">
        </div>
        <button type="submit" class="btn btn-roast btn-full">Login</button>
      </form>

      <p class="auth-switch">No account? <a href="/auth/register">Sign up</a></p>
    </div>
  </main>
</body>
</html>
